<UserControl x:Class="RaceManager.Client.Views.LogInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RaceManager.Client.Views"
             xmlns:core="clr-namespace:RaceManager.Client.Core"
             core:ViewModelLocator.AutoHookedUpViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Username" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="23" Width="150" />
            <Label Content="Password" />
            <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Height="23" Width="150" />
            <Button Content="Log In" Width="75" Command="{Binding LogInCommand}" />
            <Label Content="{Binding Username}"/>
            <Label Content="{Binding Password}"/>
        </StackPanel>
    </Grid>
</UserControl>
