<UserControl x:Class="RaceManager.Client.Views.RaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RaceManager.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366">
    
    <Grid>
        <DataGrid x:Name="racesDataGrid" ItemsSource="{Binding Races}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="idColumn" Width="Auto" Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn x:Name="eventDateColumn" Width="Auto" Header="Event Date" Binding="{Binding EventDate}"/>
                <DataGridTextColumn x:Name="eventLocationColumn" Width="Auto" Header="Event Location" Binding="{Binding EventLocation}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
